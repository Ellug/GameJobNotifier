<UserControl x:Class="GameJobNotifier.App.Views.Containers.ActivityLogContainer"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             mc:Ignorable="d">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>

        <!-- 헤더 -->
        <TextBlock Grid.Row="0"
                   Margin="0,0,0,6"
                   FontSize="13"
                   FontWeight="SemiBold"
                   Foreground="{StaticResource AccentBrush}"
                   Text="활동 로그" />

        <!-- 구분선 -->
        <Border Grid.Row="1"
                Margin="0,0,0,8"
                Height="1"
                Background="{StaticResource BorderBrushDark}" />

        <!-- 로그 목록 -->
        <ListBox x:Name="ActivityLogsList"
                 Grid.Row="2"
                 FontFamily="Consolas"
                 FontSize="12"
                 HorizontalContentAlignment="Left"
                 ItemsSource="{Binding ActivityLogs}">
            <ListBox.ItemTemplate>
                <DataTemplate>
                    <Button Command="{Binding DataContext.OpenLogItemCommand, RelativeSource={RelativeSource AncestorType=ListBox}}"
                            CommandParameter="{Binding}"
                            Background="Transparent"
                            BorderBrush="Transparent"
                            BorderThickness="0"
                            HorizontalContentAlignment="Left"
                            Padding="2"
                            Foreground="{Binding Foreground, RelativeSource={RelativeSource AncestorType=ListBox}}"
                            Cursor="Hand">
                        <TextBlock Text="{Binding Text}"
                                   TextWrapping="Wrap">
                            <TextBlock.Style>
                                <Style TargetType="TextBlock">
                                    <Setter Property="Foreground" Value="{StaticResource TextBrushPrimary}" />
                                    <Style.Triggers>
                                        <DataTrigger Binding="{Binding HasUrl}" Value="True">
                                            <Setter Property="Foreground" Value="{StaticResource AccentBrush}" />
                                            <Setter Property="TextDecorations" Value="Underline" />
                                        </DataTrigger>
                                    </Style.Triggers>
                                </Style>
                            </TextBlock.Style>
                        </TextBlock>
                    </Button>
                </DataTemplate>
            </ListBox.ItemTemplate>
        </ListBox>
    </Grid>
</UserControl>
